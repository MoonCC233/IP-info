{
  "name": "ip-info-worker",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "assets:gen": "node scripts/generate_modules.js",
    "prerender": "node scripts/prerender.js",
    "predev": "npm run assets:gen && npm run prerender",
    "predeploy": "npm run assets:gen && npm run prerender",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "@napi-rs/canvas": "^0.1.69",
    "wrangler": "^4.119.0"
  }
}
