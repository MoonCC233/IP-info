# Cloudflare Worker 配置
# 将此文件复制为 .dev.vars 用于本地开发，或在 Cloudflare Dashboard 中设置

# 可选：天气 API 配置（当前使用 Open-Meteo 免费 API，无需密钥）
# 如果需要使用其他天气服务，可以在此处添加 API 密钥

# 可选：自定义站点 URL（用于生成签名档使用说明中的链接）
SITE_URL=https://your-worker.workers.dev